# Linear Regression with PyTorch
This project demonstrates a simple linear regression model built from scratch using PyTorch. The model learns a linear relationship between input data and targets using gradient descent, and visualizes both predictions and training/testing loss curves.
